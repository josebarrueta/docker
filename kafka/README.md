# Kafka docker image

Simple Kafka image that starts with KRaft protocol, the image is based on the [https://github.com/corretto/corretto-docker/blob/7996710c56ef95dba20bb0d5784b0e941dfdaa5b/17/jdk/al2/Dockerfile|amazoncorreto:17-al2] image.

The server runs with a defined `kafka` user. 
